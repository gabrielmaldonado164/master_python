

funciones.menuPrincipal()
    # if menu == 1:
    #     try:
    #         user = usuarios.Usuario()
    #         listado = user.listarUsuarios()
    #         if len(listado) > 1:
    #             funciones.mostrarUsuarios(listado)
    #         else:
    #             print('Lo sentimos, no hay usuarios disponible en el sistema')
    #     except Exception as e:
    #         print('Error: {e}')
    # elif menu == 2:
    #     usuarios.Usuario().crearRegistro()
